Index: lede/package/utils/busybox/files/cron
===================================================================
--- lede.orig/package/utils/busybox/files/cron
+++ lede/package/utils/busybox/files/cron
@@ -29,6 +29,7 @@ start_service () {
 
 	procd_open_instance
 	procd_set_param command "$PROG" -f -c /etc/crontabs -l "${loglevel:-5}"
+	procd_set_param respawn
 	procd_close_instance
 }
 
